<Window x:Class="PriceComp.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceComp.GUI"
        mc:Ignorable="d"
        Title="Asystent Zakupowy (WPF)" Height="650" Width="1000"
        FontFamily="Segoe UI" FontSize="14">

    <Window.Resources>
        
        <SolidColorBrush x:Key="PrimaryColor" Color="#007ACC"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#0094F5"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>

        
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="5"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)" To="15" Duration="0:0:0.2"/>
                            <ColorAnimation Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" To="#0094F5" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)" To="5" Duration="0:0:0.2"/>
                            <ColorAnimation Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" To="#007ACC" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        
        <Style x:Key="ResultCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="20"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.1" BlurRadius="5"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.5"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.4" DecelerationRatio="0.5"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <TabControl Margin="10" BorderThickness="0" Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Grid>
                                    <Border Name="Border" Margin="0,0,-4,0" Background="#E0E0E0" Padding="15,10" CornerRadius="6,6,0,0">
                                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="10,2"/>
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                        <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            
            <TabItem Header="Baza Ofert">
                <Grid Background="White" Margin="0" UseLayoutRounding="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="20,20,20,10">
                        <TextBlock Text="Zarządzaj Ofertami" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                        <TextBlock Text="Przeglądaj bazę produktów dostępnych w sklepach." Foreground="#666" Margin="0,5,0,15"/>
                    </StackPanel>

                    <DataGrid x:Name="GridOffers" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="20,10,20,20"
                              BorderThickness="1" BorderBrush="#EEE" HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeaderWidth="0" Background="White">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="Background" Value="#F9F9F9"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#EEE"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" BorderBrush="Transparent" BorderThickness="0" Background="Transparent">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sklep" Binding="{Binding Store.Name}" Width="1.5*"/>
                            <DataGridTextColumn Header="Produkt" Binding="{Binding Product.Name}" Width="2*"/>
                            <DataGridTextColumn Header="Cena" Binding="{Binding Price, StringFormat={}{0:N2} zł}" Width="*"/>
                            <DataGridTextColumn Header="Jedn." Binding="{Binding Product.UnitName}" Width="0.5*"/>
                            <DataGridTextColumn Header="Cena jedn." Binding="{Binding UnitPrice, StringFormat={}{0:N2} zł}" Width="*"/>
                            <DataGridTextColumn Header="Promo" Binding="{Binding PromoPrice, TargetNullValue='', StringFormat={}{0:N2} zł}" Width="*"/>
                            <DataGridTextColumn Header="Opis Promo" Binding="{Binding PromoDescription}" Width="1.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            
            <TabItem Header="Kalkulator Koszyka">
                <Grid Background="White" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        
                        <ColumnDefinition Width="1.5*"/>
                        
                        <ColumnDefinition Width="1.5*"/>
                        
                    </Grid.ColumnDefinitions>

                    
                    <Border Grid.Column="0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Dostępne Produkty:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            <ListBox x:Name="ListProducts" Grid.Row="1" MouseDoubleClick="ListProducts_MouseDoubleClick" BorderThickness="0" Background="Transparent">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="8,5"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="#F0F0F0"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="#E0E0E0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                            <TextBlock Grid.Row="1" Text="(Kliknij dwukrotnie, aby dodać)" VerticalAlignment="Bottom" Foreground="#999" FontStyle="Italic" FontSize="12" Margin="5"/>
                        </Grid>
                    </Border>

                    
                    <Border Grid.Column="1" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Lista zakupów (po przecinku):" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                            <TextBox x:Name="TxtShoppingList" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     FontSize="15" Padding="10" Margin="0,31,0,0" Grid.RowSpan="2" Height="367" VerticalAlignment="Top"/>

                            <TextBlock Grid.Row="1" Text="Np. Cola, Masło"  Foreground="#999" FontStyle="Italic" Margin="0,372,0,103"/>
                            <Button x:Name="BtnCalculate_Copy" Panel.ZIndex="1"  Grid.Row="1" Content="OBLICZ KOSZYK" Height="50" FontSize="16" Click="BtnCalculate_Click" Margin="0,412,0,32"/>
                            

                        </Grid>
                    </Border>

                    
                    <Border Grid.Column="2"
        Padding="20"
        Background="#FAFAFA"
        BorderBrush="{StaticResource BorderColor}"
        BorderThickness="1,0,0,0"
        Margin="0,0,0,85">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                
                                <RowDefinition Height="*"/>
                                
                                <RowDefinition Height="Auto"/>
                                
                            </Grid.RowDefinitions>

                            
                            <TextBlock FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   Grid.Row="0">
            <Run Language="pl-pl" Text="Wybierz sklep"/>
            <Run Text=":"/>
                            </TextBlock>

                            
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,20"
                      Grid.Row="1">

                                <ItemsControl x:Name="ListResults">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#555"
                                Style="{StaticResource ResultCard}"
                                MouseLeftButtonDown="ChooseOffer">

                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Text="{Binding Store.Name}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Margin="0,0,0,5"/>

                                                    <StackPanel Grid.Row="1"
                                            Orientation="Horizontal"
                                            Margin="0,0,0,5">
                                                        <TextBlock Text="Suma: " Foreground="#555"/>
                                                        <TextBlock x:Name="Price_of_basket"
                                               Text="{Binding Sum, StringFormat={}{0:N2} zł}"
                                               FontWeight="Bold"
                                               Foreground="#4CAF50"/>
                                                    </StackPanel>

                                                    <ItemsControl Grid.Row="2"
                                              ItemsSource="{Binding MissingProducts}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding StringFormat='BRAK: {0}'}"
                                                       Foreground="Red"
                                                       Margin="0,2"/>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                </Grid>

                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </ScrollViewer>

                            
                            <Button x:Name="BtnGoToPayment2"
                Grid.Row="2"
                Content="PRZEJDŹ DO PŁATNOŚCI"
                Height="50"
                FontSize="16"
                Click="BtnGoToPayment"
                Margin="0,10,0,0"
                IsEnabled="False"
                Background="#FFA1A9AE"/>
                        </Grid>
                    </Border>

                </Grid>
            </TabItem>

            
            <TabItem Header="Dodaj Ofertę">
                <Grid Background="#FFF">
                    <StackPanel Margin="20" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Nowa Oferta" FontSize="24" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryColor}"/>

                        <TextBlock Text="Sklep" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="ComboStores" Margin="0,0,0,20" DisplayMemberPath="Name" Padding="8" Height="40"/>

                        <TextBlock Text="Nazwa produktu" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtNewName" Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Cena (zł)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtNewPrice"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Ilość" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtNewQty" Text="1"/>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Jednostka" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <ComboBox x:Name="ComboUnit" IsEditable="True" SelectedIndex="0" Height="36">
                                    <ComboBoxItem Content="szt"/>
                                    <ComboBoxItem Content="kg"/>
                                    <ComboBoxItem Content="l"/>
                                    <ComboBoxItem Content="pak"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>

                        <Button x:Name="BtnAddOffer" Content="DODAJ OFERTĘ" Margin="0,30,0,0" Height="45" Click="BtnAddOffer_Click"/>
                        <Button x:Name="BtnSaveNew" Content="ZAPISZ ZMIANY W BAZIE" Margin="0,15,0,0" Height="45" Click="BtnSaveFile_Click" Background="#4CAF50"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
